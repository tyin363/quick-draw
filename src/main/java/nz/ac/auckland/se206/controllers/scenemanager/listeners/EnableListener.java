package nz.ac.auckland.se206.controllers.scenemanager.listeners;

public interface EnableListener {

  /** This method is invoked just after all the fields have been injected into an instance. */
  void onEnable();
}
